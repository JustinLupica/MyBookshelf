// Book class: Represents a book
class Book {
  constructor(title, author, isbn, user_id = 1) {
    this.title = title;
    this.author = author;
    this.isbn = isbn;
    this.user_id = user_id
  }
}
